# Design Lab North

Studio site and greenhouse. We design, host while it grows, then migrate.

Local:

```bash
cd Site && npm install && npm run dev
```

# Design Lab North

Studio site and greenhouse. We design, host while it grows, then migrate.

Local:

```bash
cd Site && npm install && npm run dev
```

Open http://localhost:3010

Paper / Ink switch is in the header. Memory for agents lives in `memory/`. DNS: `ops/dns.md`.

