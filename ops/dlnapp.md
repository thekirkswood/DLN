# DLNAPP — stopped

Port **3011** is off. Ewan: pointless second brochure, not a webapp.

Folder `/home/main/DLNAPP` may still sit on disk. Do not enable `dlnapp.service`. The house is campus `:3010`.
